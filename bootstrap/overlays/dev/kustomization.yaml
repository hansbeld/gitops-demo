apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
- ../../base

# Dev-specific configurations
labels:
- includeSelectors: true
  pairs:
    cluster: dev-cluster
    environment: dev
